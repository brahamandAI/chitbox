# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/chitbox
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chitbox
DB_USER=username
DB_PASSWORD=password

# Server Configuration
PORT=3005
NODE_ENV=development

# SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=ChitBox <your-email@gmail.com>

# JWT Secret
JWT_SECRET=your-super-secret-jwt-key

# CORS Configuration
CORS_ORIGIN=http://localhost:3004

# AI Configuration (Optional)
OPENAI_API_KEY=your-openai-api-key-here

# SMTP Server Configuration (Incoming Email Server)
SMTP_SERVER_PORT=2525
SMTP_SERVER_HOST=localhost
SMTP_SERVER_BANNER=ChitBox SMTP Server

# IMAP Configuration (Our own IMAP server)
IMAP_PORT=143
IMAP_SERVER_HOST=localhost
IMAP_SERVER_BANNER=ChitBox IMAP Server
